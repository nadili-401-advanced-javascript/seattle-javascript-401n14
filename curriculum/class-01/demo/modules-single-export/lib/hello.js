"use strict";

// We use module.exports to define this
// function as a CommonJS module that we're
// exposing to whoever wants to use add this
// file to their project

module.exports = exports = name => {
	console.log(`Hello ${name}`);
};
